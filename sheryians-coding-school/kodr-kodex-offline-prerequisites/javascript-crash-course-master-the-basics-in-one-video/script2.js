/*

word vs keyword: a word which has no meaning to the translator of
the code which converts the it to machine language the are called
word and those who do have meaning then they are called keyword

*/

word
var keyword
const keyword

/*
variables: are used to store data but they can change
constant: are also used to store data but they dont change
let: 

*/

var a = 10;
let b = 20;
const c = 30;

/*
hoisting: variable and function are hoisted beccause of which
there declaration have moved on to the top of the code


note:- Interview question
 1: without initialization the declared var value is undefined 
 2: Undefined means it is present but we dont know its value and Not-defined means there is no existance of it at all
n


types in js: primitive and reference
 1: primitive: a type of value which you copy then the actual value itself is copied
        eg: numbers, string, null, undefined, boolean
 2: reference: a type of value which you copy thrn the actual value is not copied but
    the reference of it is copied when you see brackets in initialization then that means
    primitive
        eg: [], {}, ()
t


conditionals: it checks for a true and false condition and it continues untill it finds a true 
true consition and it will only run till there
    eg: if, else, else-if 


loops : use when we have to repeat same thing again and again 
        tyoes - for and while
        while runs then update the condition we have too give the just condition when it need to fail
        for check condition then run then update we give it from where to run where everything
l


function: are a block of code which had given a name which can run by calling the giving name many times
    we can use them in three wayas
        1: when you didnt want to run your code now you just want it to run in the near future
        2: when we want to reuse our code 
        3: when you want run the code but each time with different data
    in function we can change the data by passing arguments in parameters 
        argument: the real value which we give whie calling the function
        parameter: parameters are the variables which store the argument passed value 
f


array: are the variables which can store multiple values into a single variable, arrray is a group of values


push, pop, shift, unshift and splice:
    push add values at back to the array
    pop remove values from back in the array
    unshift add values at start to the array
    shift remove values from start in the array
    splice remove values from miggle with help of index and range
p


object: the data which is stored in key value pair, and you could say multiple properties of single individual
1) blank objects:
var obj = {}  
2) filled object:
var obj2 = {
    name: "nana",
    age: 22,
    area: "dehat"
}
access: obj.area
- all the keys are properties
- any key value property which stores a function as its store value thne thats called method
- change: obj.name = "MONU"



*/